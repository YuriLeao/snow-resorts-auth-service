# Copie para .env.local (não versionado) ao testar "Esqueci minha senha" no celular físico.
# Use o MESMO IP do Mac que está em snow-resorts-mobile/.env.local (EXPO_PUBLIC_API_BASE_URL).
# O profile local carrega este arquivo automaticamente ao rodar ./mvnw spring-boot:run.

PASSWORD_RESET_BASE_URL=http://192.168.3.18:8080/reset-password
